// replies.js
export function generateReplies(message) {
  // simple example — return an array of canned responses
  return [
    `You said: "${message}". Interesting!`,
    "Thanks for your message!",
    "Got it, let me think about that…",
    "I'll get back to you ASAP!"
  ];
}
